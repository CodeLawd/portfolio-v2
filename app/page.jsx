import Dashboard from "./Dashboard";

export const metadata = {
  title: "Joshua Alexander's Portfolio",
};

const Home = () => {
  return (
    <div className="bg-primary">
      <Dashboard />
    </div>
  );
};

export default Home;
